{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.3.1"
  },
  "scripts": {
    "publish": "lerna publish",
    "bootstrap_hoist": "lerna bootstrap --hoist",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean",
    "storybook": "lerna run storybook",
    "project": "lerna run start --scope=project"
  }
}
